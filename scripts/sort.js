document.addEventListener('DOMContentLoaded', () => {
	const title = document.querySelector('.section-many-notebook__red');
	let array = ['q', 'w', 'e', 'r', 't', 'y'];

	array.forEach((e)=>{
		e = new Array();
	});
	/*window.addEventListener('scroll', () => {

	});*/
});